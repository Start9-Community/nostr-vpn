repository: https://git.iris.to/#/npub1xdhnr9mrv47kkrn95k6cwecearydeh8e895990n3acntwvmgk2dsdeeycm/nostr-vpn
pubkey: npub1xdhnr9mrv47kkrn95k6cwecearydeh8e895990n3acntwvmgk2dsdeeycm
# Deliberately nonexistent in the tracked checkout. The fleet-gated local
# release command copies the manifest-bound APK into a private immutable
# directory and rewrites this field in a private one-shot config. Running zsp
# directly therefore cannot accidentally publish a stale same-version APK.
release_source: .release-gate-required/no-direct-zsp.apk

name: Nostr VPN
summary: Mesh VPN built on Nostr signaling and userspace WireGuard.
description: |
  Nostr VPN is a Tailscale-style mesh VPN for your own devices and trusted peers.

  Nostr is used for peer discovery and encrypted signaling; the data plane
  is userspace WireGuard, with the FIPS overlay handling NAT traversal and
  transit through friend-of-a-friend nodes when direct paths are blocked.

  Current app capabilities:
  - private mesh networking between approved participants
  - admin-approved signed join requests
  - exit nodes and route advertisement (incl. WireGuard upstream)
  - MagicDNS-style `.nvpn` naming inside the mesh
  - nearby signed-request pairing when relay delivery is unavailable

website: https://git.iris.to/#/npub1xdhnr9mrv47kkrn95k6cwecearydeh8e895990n3acntwvmgk2dsdeeycm/nostr-vpn
icon: ios/Resources/Assets.xcassets/AppIcon.appiconset/AppIcon-1024.png
license: MIT
tags:
  - nostr
  - vpn
  - wireguard
  - privacy
  - mesh
metadata_sources:
  - github
